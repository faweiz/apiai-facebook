
version: "2.0"
test:
  image: daocloud/ci-node:0.12
  env:
  - PORT = 5000
  install:
  - npm install .
  script:
  - npm run-script test
build:
  image:
    dockerfile_path: /Dockerfile
    build_dir: /
    cache: true
